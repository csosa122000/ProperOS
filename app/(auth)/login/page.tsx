'use client';
import Link from 'next/link';
import { FormEvent, useState } from 'react';
import { createClient } from '@/lib/supabase/client';
export default function Login(){const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const fd=new FormData(e.currentTarget);const supabase=createClient();const {error}=await supabase.auth.signInWithPassword({email:String(fd.get('email')),password:String(fd.get('password'))});if(error){setError(error.message);setLoading(false);return;}await supabase.rpc('claim_pending_memberships');window.location.href='/dashboard';}return <main className="auth"><section className="auth-brand"><h1>Proper OS</h1><p>Run sales, estimating, proposals, production, accounting, and customer operations from one system.</p></section><section className="auth-panel"><form className="card login-card" onSubmit={submit}><h2>Sign in</h2><p>Access your Proper Remodeling workspace.</p><label className="field">Email<input name="email" type="email" autoComplete="email" required defaultValue="sosa_chris@icloud.com"/></label><label className="field">Password<input name="password" type="password" autoComplete="current-password" required/></label><p style={{textAlign:'right',marginTop:'-8px'}}><Link href="/forgot-password">Forgot password?</Link></p>{error&&<p className="error">{error}</p>}<button className="primary" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form></section></main>}

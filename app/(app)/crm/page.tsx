import { createClient } from '@/lib/supabase/server';
export default async function CRM(){const supabase=await createClient();const {data:orgs}=await supabase.rpc('get_my_organizations');const orgId=orgs?.[0]?.organization_id;const {data:leads}=orgId?await supabase.from('leads').select('*').eq('organization_id',orgId).is('deleted_at',null).order('created_at',{ascending:false}):{data:[]};return <><div className="top"><div><h1>CRM</h1><p>Leads, customers, properties, appointments, notes, and tasks.</p></div><button className="primary" style={{width:'auto'}}>New Lead</button></div><div className="card"><table className="table"><thead><tr><th>Name</th><th>Phone</th><th>Project</th><th>Status</th></tr></thead><tbody>{leads?.length?leads.map((l:any)=><tr key={l.id}><td>{l.first_name} {l.last_name}</td><td>{l.phone||'—'}</td><td>{l.project_interest?.join(', ')||'—'}</td><td><span className="pill">{l.status}</span></td></tr>):<tr><td colSpan={4}>No leads yet.</td></tr>}</tbody></table></div></>}

import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';
const links=[['Dashboard','/dashboard'],['CRM','/crm'],['Estimates','/estimates'],['Proposals','/proposals'],['Production','/production'],['Settings','/settings']];
export async function AppShell({children}:{children:React.ReactNode}){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();const {data:orgs}=await supabase.rpc('get_my_organizations');return <div className="shell"><aside className="sidebar"><div className="brand">Proper OS</div><div className="org"><small>Company</small><div>{orgs?.[0]?.organization_name||'Proper Remodeling'}</div></div><nav className="nav">{links.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}</nav></aside><main className="main"><header className="top"><div><strong>Proper Remodeling</strong><div style={{color:'#6b7280'}}>{user?.email}</div></div><div className="actions"><button className="secondary">Notifications</button></div></header>{children}</main></div>}
